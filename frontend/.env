# Base path for the FastAPI backend. In dev, Vite proxies this to http://localhost:8000.
VITE_API_BASE=/api/v1

# When true, the app serves data from src/api/mocks instead of the real backend.
# Lets the frontend run fully standalone (parallel scaffolding) before the backend is live.
VITE_USE_MOCKS=False
